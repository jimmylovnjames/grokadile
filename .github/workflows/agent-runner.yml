name: Agent Runner CI

on:
  push:
    branches: [ main, "claude/**" ]
    paths: [ "agent-runner/**", ".github/workflows/agent-runner.yml" ]
  pull_request:
    paths: [ "agent-runner/**", ".github/workflows/agent-runner.yml" ]
  workflow_dispatch:

permissions:
  contents: read

defaults:
  run:
    working-directory: agent-runner

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: agent-runner/package-lock.json

      - name: Install
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Test
        run: npm test
